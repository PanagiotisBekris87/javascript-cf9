// var, let, const
// Το var δεν το χρησιμοποιουμε πια
let counter = 0; // this vvariable can be re-assigned
const PI = 3.14; // this vvariable cannot be re-assigned

counter++; // Incrementing

console.log("Counter:", counter);
console.log("PI:", PI);
